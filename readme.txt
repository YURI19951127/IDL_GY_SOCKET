IDL_GY_SOCKET是用于全向可视度仪数据传输、处理软件。
采用socket套接字协议方式，全向可视度仪为服务器，PC为客户端。以json格式发送请求和接收数据。
开发问题：1、IDL读写文件方式需要打开文件句柄。
         2、目前已知主流函数为open,read,write。
         3、文件读写不同于python，在不知道数据长度情况下无法依靠判断得到句尾，即不能使用不定长数组
         4、readf方式不限数据长度，但只能读一行。readu需要知道文件长度
开发进度：